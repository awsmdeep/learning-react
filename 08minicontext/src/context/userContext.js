import React, { createContext } from "react"; // Importing React and createContext from the 'react' library

// Creating a new context object using createContext() method provided by React
const userContext = createContext();

export default userContext; // Exporting the userContext object as the default export
